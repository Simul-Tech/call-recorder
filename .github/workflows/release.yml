name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
          - name: Linux x86_64
            runner: ubuntu-latest
            goos: linux
            goarch: amd64
            asset: call-recorder-linux-amd64
            tags: ""

          - name: Linux x86_64 (CLI)
            runner: ubuntu-latest
            goos: linux
            goarch: amd64
            asset: call-recorder-linux-amd64-cli
            tags: notray

          - name: Linux ARM64
            runner: ubuntu-latest
            goos: linux
            goarch: arm64
            asset: call-recorder-linux-arm64
            tags: ""

          - name: Linux ARM64 (CLI)
            runner: ubuntu-latest
            goos: linux
            goarch: arm64
            asset: call-recorder-linux-arm64-cli
            tags: notray

          - name: Windows x86_64
            runner: windows-latest
            goos: windows
            goarch: amd64
            asset: call-recorder-windows-amd64.exe
            tags: ""

          - name: macOS Apple Silicon
            runner: macos-14
            goos: darwin
            goarch: arm64
            asset: call-recorder-macos-arm64
            tags: ""

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Install Linux ARM64 cross-compiler
        if: matrix.goos == 'linux' && matrix.goarch == 'arm64'
        run: sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Install Linux tray build deps
        if: matrix.goos == 'linux' && matrix.tags == ''
        run: sudo apt-get update && sudo apt-get install -y --fix-missing libayatana-appindicator3-dev

      - name: Build
        shell: bash
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "1"
        run: |
          [ "$GOARCH" = "arm64" ] && [ "$GOOS" = "linux" ] && export CC=aarch64-linux-gnu-gcc
          TAGS_FLAG=""
          [ -n "${{ matrix.tags }}" ] && TAGS_FLAG="-tags ${{ matrix.tags }}"
          go build -ldflags="-s -w" $TAGS_FLAG -o "${{ matrix.asset }}" .

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset }}
          path: ${{ matrix.asset }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          files: "call-recorder-*"
          generate_release_notes: true
